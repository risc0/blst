[package]
name = "bls"
version = "0.1.0"
edition = "2021"

[profile.release]
codegen-units = 1
lto = "fat"

[dependencies]
blst = { path = "../../../." }
risc0-zkvm = { version = "2.0", default-features = false, features = ["std", "unstable"] }
